# =============================================================================
# GAPI — Python dependencies
# =============================================================================
#
# Quick install (core + web GUI only):
#   pip install -r requirements.txt
#
# Full install (everything including optional integrations):
#   pip install -r requirements-full.txt
#
# =============================================================================

# -----------------------------------------------------------------------------
# Core — required for the web GUI (gapi_gui.py) and CLI (gapi.py)
# Python 3.8+ required (discord.py and asyncio features need 3.8+)
# -----------------------------------------------------------------------------
requests>=2.28.0
colorama>=0.4.6
flask>=2.0.0
python-dotenv>=1.0.0

# Database (PostgreSQL — required for multi-user web GUI)
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0      # swap for psycopg2 if you build from source

# -----------------------------------------------------------------------------
# Optional — Discord bot (discord_bot.py)
# -----------------------------------------------------------------------------
discord.py>=2.3.0

# -----------------------------------------------------------------------------
# Optional — Platform integrations
# -----------------------------------------------------------------------------
epicstore-api>=0.1.7          # Epic Games Store library sync
# galaxy.plugin.api>=0.69,<0.70  # GOG Galaxy (uncomment if GOG integration needed)

# -----------------------------------------------------------------------------
# Optional — Extra features
# -----------------------------------------------------------------------------
howlongtobeatpy>=1.0.0        # How Long to Beat completion-time estimates
pypresence>=4.3.0             # Discord Rich Presence (shows picked game in Discord)
graphene>=3.0.0               # GraphQL API (POST /api/graphql)
